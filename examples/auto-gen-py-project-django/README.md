# auto-gen-py-project-django

Example plugin that registers the `django-extended` template.

## Install

```bash
pip install -e ./examples/auto-gen-py-project-django
```

## Use

```bash
auto-gen-py-project new Demo -t django-extended
```
