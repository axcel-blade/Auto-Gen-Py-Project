# auto-gen-py-project-ai

Example plugin that registers the `ai-extended` template (offline pipeline stub — no LLM calls).

## Install

```bash
pip install -e ./examples/auto-gen-py-project-ai
```

## Use

```bash
auto-gen-py-project new Demo -t ai-extended
```
