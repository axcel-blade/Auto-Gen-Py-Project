# {{ name }}

{{ description }}

Generated with **auto-gen-py-project** template `ai-extended` (offline stub — no LLM).

## Quick start

```bash
python -m pip install -e ".[dev]"
python -m {{ package_name }}
```
